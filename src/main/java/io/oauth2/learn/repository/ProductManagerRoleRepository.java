package io.oauth2.learn.repository;

import io.oauth2.learn.model.ProductManagerRole;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductManagerRoleRepository extends JpaRepository<ProductManagerRole,Long> {
}
